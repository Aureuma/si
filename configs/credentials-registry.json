{
  "version": 1,
  "secrets": [
    {
      "name": "app-releaseparty",
      "file": "app-ReleaseParty.env.sops",
      "format": "env",
      "allow_any_key": true,
      "description": "ReleaseParty app env secrets (SOPS encrypted)."
    }
  ]
}
