FROM node:22-bookworm
WORKDIR /workspace/apps
# Placeholder for interactive LLM tooling; install inside container as needed (e.g., npm i -g @openai/codex-cli once available).
CMD ["tail", "-f", "/dev/null"]
