# Copy to .env and set Telegram chat for notifications (optional; can be set per message)
TELEGRAM_CHAT_ID=245579957

# Optional: SSH destination used in tunnel commands sent to humans (best is "<user>@<public_ip>")
SSH_TARGET=shawn@65.19.188.111

# Optional: Swarm stack and overlay network names.
SILEXA_STACK=silexa
SILEXA_NETWORK=silexa_net
