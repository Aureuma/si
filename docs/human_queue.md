# Human Action Queue

Append blocking tasks for humans here. Format:

- [ ] **Task**: <short description>
  - **Command(s) for human**: <exact commands to run locally>
  - **URL to open**: <URL>
  - **Window/Timeout**: <e.g., 15 minutes>
  - **Requested by**: <actor/critic name>
  - **Timestamp (UTC)**: <yyyy-mm-dd hh:mm:ss>
  - **Notes**: <extra context>

Mark done by changing `[ ]` to `[x]` with the finisher’s initials and time.

Example Codex CLI login task:
- [ ] **Task**: Complete Codex CLI OAuth for dyad `web`
  - **Command(s) for human**: `kubectl -n silexa port-forward pod/<pod> 47123:47124`
  - **URL to open**: `http://127.0.0.1:47123/auth/continue?...`
  - **Window/Timeout**: 15 minutes
  - **Requested by**: web
  - **Timestamp (UTC)**: 2025-12-14 01:10:00
  - **Notes**: Run the command on a machine with a browser; keep the port-forward alive until OAuth finishes.

- [ ] **Task**: Codex CLI login for dyad `infra`
  - **Command(s) for human**: `bin/run-task.sh infra bash -lc 'export OPENAI_API_KEY=<your_key>; codex login'`
  - **URL to open**: _Generated by codex login; use port-forward if remote_
  - **Window/Timeout**: 20 minutes
  - **Requested by**: infra
  - **Timestamp (UTC)**: 2025-12-16 15:32:27
  - **Notes**: When Codex prints a callback port, run `kubectl port-forward pod/<pod> <LOCAL_PORT>:<FORWARD_PORT>` and complete OAuth.

- [ ] **Task**: Provide GitHub PAT for MCP gateway
  - **Command(s) for human**: `printf '%s' <github_pat> | sudo tee /opt/silexa/secrets/gh_token >/dev/null && cd /opt/silexa && kubectl -n silexa create secret generic mcp-gateway-secrets --from-file=gh_token=secrets/gh_token --dry-run=client -o yaml | kubectl apply -f - && bin/mcp-gateway-up.sh`
  - **URL to open**: n/a
  - **Window/Timeout**: 15 minutes
  - **Requested by**: ops
  - **Timestamp (UTC)**: 2025-12-16 16:18:43
  - **Notes**: Token needs repo scope at minimum. Prefer secret file/env on host; do not commit the token.

- [ ] **Task**: Provide Stripe API key for MCP gateway
  - **Command(s) for human**: `printf '%s' <stripe_secret_key> | sudo tee /opt/silexa/secrets/stripe_api_key >/dev/null && cd /opt/silexa && kubectl -n silexa create secret generic mcp-gateway-secrets --from-file=stripe_api_key=secrets/stripe_api_key --dry-run=client -o yaml | kubectl apply -f - && bin/mcp-gateway-up.sh`
  - **URL to open**: n/a
  - **Window/Timeout**: 15 minutes
  - **Requested by**: ops
  - **Timestamp (UTC)**: 2025-12-16 16:18:46
  - **Notes**: Use the appropriate test/live key; do not commit the key. Unlocks Stripe MCP via the gateway.
